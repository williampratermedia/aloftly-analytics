# =============================================================================
# Aloftly Analytics — Environment Variables
# Copy this file to .env.local and fill in real values for local development.
# Production values are set in Vercel dashboard.
# =============================================================================

# Node environment
NODE_ENV=development

# -----------------------------------------------------------------------------
# Supabase
# Dashboard -> Project Settings -> API
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# Database (Drizzle ORM)
# Dashboard -> Project Settings -> Database -> Connection String
# DATABASE_URL      = pooled (Transaction mode via PgBouncer) — app queries
# DATABASE_URL_DIRECT = direct (non-pooled) — migrations only
# -----------------------------------------------------------------------------
DATABASE_URL=
DATABASE_URL_DIRECT=

# -----------------------------------------------------------------------------
# Sentry Error Monitoring
# Dashboard -> Project Settings -> Client Keys (DSN)
# Dashboard -> Organization Settings -> Organization Slug (SENTRY_ORG)
# Dashboard -> Project Settings -> Project Slug (SENTRY_PROJECT)
# Dashboard -> Settings -> Auth Tokens (SENTRY_AUTH_TOKEN)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
